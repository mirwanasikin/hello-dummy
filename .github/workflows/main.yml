name: Random hello

on:
  push:
    branches: ["main"]

jobs:
  echo:
    runs-on: ubuntu-latest

    steps:
      - name: Say hello
        run: echo "Hello World"

      - name: Say I Love Linux
        run: |
          echo "I Love Linux" \
          echo "I Love NixOS" \
          echo "I Use NixOS, BTW" \
